#include <stdio.h>
/**
 * main - starting point for execution
 *
 * Return: ends execution of a function
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
